const MacusUtils=(()=>{const padId=n=>String(n).padStart(6,"0");const isoNow=()=>new Date().toISOString();const formatDate=iso=>iso?new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short"}).format(new Date(iso)):"-";const parseTags=value=>value.split(",").map(tag=>tag.trim()).filter(Boolean).filter((tag,index,arr)=>arr.findIndex(t=>t.toLowerCase()===tag.toLowerCase())===index);const escapeHTML=(value="")=>value.replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]));const sameWeek=iso=>{if(!iso)return false;const date=new Date(iso);const now=new Date();const start=new Date(now);start.setDate(now.getDate()-now.getDay());start.setHours(0,0,0,0);return date>=start};return{padId,isoNow,formatDate,parseTags,escapeHTML,sameWeek}})();